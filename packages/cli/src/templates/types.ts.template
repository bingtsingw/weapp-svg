import type { CSSProperties } from 'react';

export type IconSize = CSSProperties['width'];

export interface IconProps {
  size?: IconSize;
  color?: string | string[];
  style?: CSSProperties;
  className?: string;
}
