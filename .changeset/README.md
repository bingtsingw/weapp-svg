# Changesets

This directory records release notes and semantic version bumps for publishable packages.

Run `pnpm changeset-add` for a change that should be released. The release workflow consumes these files when it creates the version pull request.
